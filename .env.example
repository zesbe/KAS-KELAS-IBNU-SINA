# Monorepo Environment Configuration
# Copy this file to .env and fill in your values

# Note: This monorepo contains both frontend and backend
# Each workspace has its own .env file:
# - frontend/.env for React app environment variables
# - backend/.env for Node.js server environment variables

# See frontend/.env.example and backend/.env.example for specific configurations